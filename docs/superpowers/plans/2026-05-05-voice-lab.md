# Voice Lab 试听对比室 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在交互式终端中把"选音色 → 选风格 → 试听单段"的串行流程，替换为"批量合成多组合 → 自由对比 → 临时设候选 → 最终确认"的试听对比室；同时让试听文本来自正文随机段落，而非版权页/目录。

**Architecture:** 新建独立模块 `voice_lab.py`，对外只暴露 `run_voice_lab(file_path, language) -> tuple[str, str]`。内部分四个函数：`_sample_preview_text`（智能采样）、`_select_combos`（勾选组合）、`_synthesize_previews`（并发合成到临时 mp3）、`_interactive_compare`（菜单循环）。`main.py` 删除旧的 `_preview_sample` / `_select_voice` / `_select_style`，改为单次调用 `voice_lab.run_voice_lab(...)`。所有依赖（`parser` / `rule_cleaner` / `synthesizer` / `assembler`）原样复用。

**Tech Stack:** Python 3.12 · `questionary`（交互式 CLI）· `rich`（表格/输出）· `pydub`（音频拼接）· `pytest`（测试，已在 venv 中）

---

## File Structure

| 文件 | 类型 | 职责 |
|---|---|---|
| `voice_lab.py` | **新建** | 试听对比室全部业务逻辑；唯一对外入口 `run_voice_lab(file_path, language)` |
| `main.py` | **修改** | 删除 `_preview_sample` / `_select_voice` / `_select_style`；`interactive_mode` 中调用 `voice_lab.run_voice_lab` |
| `tests/__init__.py` | **新建** | 空文件，使 `tests` 成为 package |
| `tests/conftest.py` | **新建** | pytest 路径配置（添加项目根到 sys.path，便于 import voice_lab） |
| `tests/test_voice_lab.py` | **新建** | voice_lab 单元/集成测试 |

**重要符号一览**（先看这里再读任务，所有任务都按这套签名实现）：

```python
# voice_lab.py
from dataclasses import dataclass
from pathlib import Path

@dataclass
class PreviewItem:
    voice: str
    style: str            # 'default' | 'news' | 'story' | 'biography' | 'nonfiction'
    style_label: str      # 显示名，如 '默认（平静温暖）'
    mp3_path: Path | None # 合成成功才有
    error: str | None     # 失败信息（短）
    duration_sec: float   # 合成完写入；失败时 0.0

def _sample_preview_text(book: BookData, target_chars: int = 200) -> str: ...
def _truncate_at_sentence(text: str, target: int) -> str: ...
def _select_combos(language: str) -> list[tuple[str, str]]: ...
async def _synthesize_previews(
    text: str, language: str, combos: list[tuple[str, str]], tmp_dir: Path
) -> list[PreviewItem]: ...
async def _interactive_compare(
    items: list[PreviewItem], language: str
) -> tuple[str, str]: ...
async def run_voice_lab(
    file_path: Path, language: str
) -> tuple[str, str]: ...
```

> ⚠️ 项目数据模型类名是 `BookData`（来自 `models.py`），**不是** `Book`。所有 type hint / 测试都用 `BookData`。

---

## Task 1: 测试脚手架

**Files:**
- Create: `tests/__init__.py`
- Create: `tests/conftest.py`
- Test: 通过 `pytest --collect-only` 验证

- [ ] **Step 1: 创建 `tests/__init__.py`（空文件）**

```bash
mkdir -p /Users/hainingyu/Code/auto_audiobook/tests
touch /Users/hainingyu/Code/auto_audiobook/tests/__init__.py
```

- [ ] **Step 2: 创建 `tests/conftest.py`**

```python
"""pytest 配置 — 把项目根加入 sys.path，使测试可以 `import voice_lab` 等顶层模块。"""
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
if str(ROOT) not in sys.path:
    sys.path.insert(0, str(ROOT))
```

- [ ] **Step 3: 验证 pytest 能发现测试目录**

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/pytest --collect-only tests/ 2>&1 | tail -5`

Expected:
```
no tests ran in 0.01s
```
（或类似无错误信息——重点是 collect 不抛 import error）

- [ ] **Step 4: Commit**

```bash
git -C /Users/hainingyu/Code/auto_audiobook add tests/__init__.py tests/conftest.py
git -C /Users/hainingyu/Code/auto_audiobook commit -m "test: scaffold tests/ directory"
```

---

## Task 2: `_truncate_at_sentence` 自然边界截断

**Files:**
- Create: `voice_lab.py`
- Test: `tests/test_voice_lab.py`

- [ ] **Step 1: 写失败测试 — 句末标点截断**

写入 `tests/test_voice_lab.py`：

```python
"""voice_lab 单元/集成测试。"""
from voice_lab import _truncate_at_sentence


def test_truncate_at_sentence_finds_chinese_period():
    text = "这是第一句话。这是第二句话。这是第三句话。这是第四句话。"
    out = _truncate_at_sentence(text, target=8)
    # target=8，区间 [5.6, 10.4]，第一个句号在 index 7（"这是第一句话。" 长 7）
    assert out == "这是第一句话。"


def test_truncate_at_sentence_returns_full_when_short():
    text = "短文本。"
    assert _truncate_at_sentence(text, target=200) == "短文本。"


def test_truncate_at_sentence_falls_back_to_hard_cut_when_no_punctuation():
    text = "abcdefghijklmnopqrstuvwxyz"
    # 无句末标点，硬截断到 target
    assert _truncate_at_sentence(text, target=10) == "abcdefghij"


def test_truncate_at_sentence_handles_english_period():
    text = "This is sentence one. This is sentence two. This is sentence three."
    out = _truncate_at_sentence(text, target=25)
    # 区间 [17.5, 32.5]，"This is sentence one." 长 21
    assert out == "This is sentence one."
```

- [ ] **Step 2: 运行测试，确认全部失败**

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/pytest tests/test_voice_lab.py -v`
Expected: 4 ERRORS（`ModuleNotFoundError: No module named 'voice_lab'`）

- [ ] **Step 3: 创建 `voice_lab.py` 含最小实现**

写入 `/Users/hainingyu/Code/auto_audiobook/voice_lab.py`：

```python
"""试听对比室 — 批量合成多组合，让用户横向比较音色与风格。"""
from __future__ import annotations

import re

# 中英文句末标点；负向先行避免切断中文引号内的句号
_SENTENCE_END = re.compile(r'[。！？!?\.](?=[^"」』]|$)')


def _truncate_at_sentence(text: str, target: int) -> str:
    """在 [target*0.7, target*1.3] 区间内寻找最近句末标点截断。

    无合适标点时硬截断到 target。
    """
    if len(text) <= target:
        return text
    window = text[: int(target * 1.3)]
    cuts = [m.end() for m in _SENTENCE_END.finditer(window)]
    cuts = [c for c in cuts if c >= target * 0.7]
    if cuts:
        return text[: cuts[0]]
    return text[:target]
```

- [ ] **Step 4: 运行测试，确认全部通过**

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/pytest tests/test_voice_lab.py -v`
Expected: 4 passed

- [ ] **Step 5: Commit**

```bash
git -C /Users/hainingyu/Code/auto_audiobook add voice_lab.py tests/test_voice_lab.py
git -C /Users/hainingyu/Code/auto_audiobook commit -m "feat(voice_lab): truncate text at natural sentence boundary"
```

---

## Task 3: `_sample_preview_text` 智能采样

**Files:**
- Modify: `voice_lab.py`
- Test: `tests/test_voice_lab.py`

- [ ] **Step 1: 写失败测试 — 过滤目录章节、回退、拼接**

把以下测试**追加**到 `tests/test_voice_lab.py`：

```python
import random

import pytest

from models import BookData, Chapter
from voice_lab import _sample_preview_text


def _make_book(chapters: list[tuple[str, list[str]]]) -> BookData:
    return BookData(
        title="t",
        author="a",
        language="zh",
        chapters=[Chapter(title=t, chunks=cs) for t, cs in chapters],
    )


def test_sample_preview_text_filters_short_titles_and_short_chapters(monkeypatch):
    # 前两章是"目录式"（短标题或短内容），应该被过滤
    book = _make_book([
        ("目", ["x" * 50]),                      # 标题过短
        ("封面", ["short"]),                     # 内容过短
        ("第一章 旅程开始", ["正" * 600]),        # 合格
        ("第二章 风暴", ["浪" * 600]),            # 合格
    ])
    # 固定随机种子以确保可重现
    monkeypatch.setattr(random, "choice", lambda seq: seq[0])
    out = _sample_preview_text(book, target_chars=100)
    # 应从第三章（过滤后第一个）抽取
    assert out.startswith("正")


def test_sample_preview_text_falls_back_when_all_filtered(monkeypatch):
    # 所有章节都"短"，回退到原始章节列表
    book = _make_book([
        ("a", ["A" * 30]),
        ("b", ["B" * 30]),
    ])
    monkeypatch.setattr(random, "choice", lambda seq: seq[0])
    out = _sample_preview_text(book, target_chars=200)
    assert out == "A" * 30


def test_sample_preview_text_pads_when_first_chunk_too_short(monkeypatch):
    # 首个 chunk < 50 字，应拼接同章后续 chunks
    book = _make_book([
        ("第一章 长正文", ["abc", "defghij" * 10]),  # chunk0 长 3，chunk1 长 70
    ])
    monkeypatch.setattr(random, "choice", lambda seq: seq[0])
    out = _sample_preview_text(book, target_chars=100)
    assert out.startswith("abc")
    assert len(out) >= 50


def test_sample_preview_text_does_not_mutate_book(monkeypatch):
    """采样不应修改原 BookData 对象（防回归：早期版本曾改 chapter.chunks）。"""
    book = _make_book([
        ("第一章 长正文", ["abc", "d" * 100]),
    ])
    original_chunks = list(book.chapters[0].chunks)
    monkeypatch.setattr(random, "choice", lambda seq: seq[0])
    _sample_preview_text(book, target_chars=200)
    assert book.chapters[0].chunks == original_chunks


def test_sample_preview_text_returns_empty_when_chunks_all_empty(monkeypatch):
    book = _make_book([
        ("第一章 标题够长但无内容", []),
        ("第二章 标题够长但无内容", []),
    ])
    # 让 random.choice 总返回第一个（chunks=[]）
    monkeypatch.setattr(random, "choice", lambda seq: seq[0])
    out = _sample_preview_text(book, target_chars=200)
    assert out == ""
```

- [ ] **Step 2: 运行测试，确认 5 个新测试失败**

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/pytest tests/test_voice_lab.py -v`
Expected: 4 passed (Task 2) + 5 errors（`ImportError: cannot import name '_sample_preview_text'`）

- [ ] **Step 3: 在 `voice_lab.py` 实现 `_sample_preview_text`**

在 `voice_lab.py` 顶部追加 imports，并在文件末尾追加函数：

```python
import random

from models import BookData
```

```python
def _sample_preview_text(book: BookData, target_chars: int = 200) -> str:
    """从书中智能抽取一段试听文本。

    规则：
    1. 过滤"目录式"章节（标题<=2字 或 章节总文本<=200字）
    2. 全部被过滤则回退到原始章节列表
    3. 随机选一章；最多重抽 3 次以避开 chunks 为空的章节
    4. 取首个 chunk 在自然边界截断到 ~target_chars
    5. 不足 50 字时,拼接同章后续 chunk（不修改原对象）
    """
    content_chapters = [
        ch for ch in book.chapters
        if len(ch.title) > 2 and sum(len(c) for c in ch.chunks) > 200
    ]
    if not content_chapters:
        content_chapters = book.chapters

    chapter = None
    for _ in range(3):
        candidate = random.choice(content_chapters)
        if candidate.chunks:
            chapter = candidate
            break
    if chapter is None:
        return ""

    text = _truncate_at_sentence(chapter.chunks[0], target_chars)

    idx = 1
    while len(text) < 50 and idx < len(chapter.chunks):
        text += chapter.chunks[idx][: target_chars - len(text)]
        idx += 1
    return text
```

- [ ] **Step 4: 运行所有测试**

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/pytest tests/test_voice_lab.py -v`
Expected: 9 passed

- [ ] **Step 5: Commit**

```bash
git -C /Users/hainingyu/Code/auto_audiobook add voice_lab.py tests/test_voice_lab.py
git -C /Users/hainingyu/Code/auto_audiobook commit -m "feat(voice_lab): smart preview text sampling that skips TOC chapters"
```

---

## Task 4: `_select_combos` 勾选组合

**Files:**
- Modify: `voice_lab.py`
- Test: `tests/test_voice_lab.py`

> 该函数包裹 `questionary.checkbox`。我们用 monkeypatch 替换 `questionary` 模块对象本身的属性来测试默认勾选与上限提示。

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_voice_lab.py`：

```python
import voice_lab as vl_module


def test_select_combos_default_includes_all_voices_with_default_style(monkeypatch):
    """默认勾选 = 当前语言下所有音色 × 'default' 风格。"""
    captured = {}

    class _FakeCheckbox:
        def __init__(self, _msg, choices, **kw):
            captured["choices"] = choices

        def ask(self):
            # 模拟用户原样接受默认勾选
            return [c.value for c in captured["choices"] if c.checked]

    class _FakeConfirm:
        def __init__(self, _msg, default=True):
            self.default = default

        def ask(self):
            return True

    monkeypatch.setattr(vl_module.questionary, "checkbox", _FakeCheckbox)
    monkeypatch.setattr(vl_module.questionary, "confirm", _FakeConfirm)

    combos = vl_module._select_combos("zh")

    # 中文有 4 个音色（茉莉/白桦/苏打/冰糖），所有默认风格 = 4 个组合
    assert len(combos) == 4
    assert all(style == "default" for _, style in combos)
    voice_names = {v for v, _ in combos}
    assert voice_names == {"茉莉", "白桦", "苏打", "冰糖"}


def test_select_combos_warns_when_over_twenty(monkeypatch):
    """超 20 个组合时应弹 confirm。"""
    confirm_calls = []

    class _FakeCheckbox:
        def __init__(self, _msg, choices, **kw):
            self._choices = choices

        def ask(self):
            # 用户全选（4 音色 × 5 风格 = 20，刚好不触发；构造 21 触发）
            return [c.value for c in self._choices]

    class _FakeConfirm:
        def __init__(self, _msg, default=True):
            confirm_calls.append(_msg)

        def ask(self):
            return True

    monkeypatch.setattr(vl_module.questionary, "checkbox", _FakeCheckbox)
    monkeypatch.setattr(vl_module.questionary, "confirm", _FakeConfirm)

    # 临时往 zh 多塞一个音色，使全选 = 5 × 5 = 25 > 20
    import config
    original = list(config.TTS_VOICE_OPTIONS["zh"])
    monkeypatch.setattr(
        config,
        "TTS_VOICE_OPTIONS",
        {**config.TTS_VOICE_OPTIONS,
         "zh": original + [{"name": "测试音", "gender": "female", "label": "test"}]},
    )

    combos = vl_module._select_combos("zh")
    assert len(combos) == 25
    assert any("勾选" in msg or "组合" in msg for msg in confirm_calls)
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/pytest tests/test_voice_lab.py -v -k select_combos`
Expected: 2 errors（`AttributeError: module 'voice_lab' has no attribute '_select_combos'` 或 `questionary` 未导入）

- [ ] **Step 3: 实现 `_select_combos`**

在 `voice_lab.py` 顶部追加 imports：

```python
import questionary

import config
```

> `STYLE_LABELS` 已在 `main.py` 中定义；不重复定义，从 main 导入会形成循环依赖。改为在 `voice_lab.py` 内**重新声明一份**等价常量（YAGNI 例外：风格名是配置数据，跨模块共享反而会绑死）。

在文件末尾追加：

```python
# 与 main.py 的 STYLE_LABELS 同步；voice_lab 独立声明避免循环依赖
_STYLE_LABELS_ZH = {
    "default": "默认（平静温暖）",
    "news": "新闻播报（权威专业）",
    "story": "故事叙述（富有感情）",
    "biography": "传记叙述（客观平实）",
    "nonfiction": "知识讲解（清晰逻辑）",
}
_STYLE_LABELS_EN = {
    "default": "Default (calm & warm)",
    "news": "News (authoritative)",
    "story": "Story (emotional)",
    "biography": "Biography (objective)",
    "nonfiction": "Non-fiction (clear logic)",
}


def _style_labels(language: str) -> dict[str, str]:
    return _STYLE_LABELS_EN if language == "en" else _STYLE_LABELS_ZH


def _select_combos(language: str) -> list[tuple[str, str]]:
    """让用户勾选 (voice, style) 组合。返回选定的列表。

    默认勾选：当前语言下所有音色 × 'default' 风格。
    超 20 个时弹 confirm 防误操作（软提示，不强阻断）。
    """
    voices = config.TTS_VOICE_OPTIONS.get(language, config.TTS_VOICE_OPTIONS["zh"])
    styles = _style_labels(language)

    choices = []
    for v in voices:
        for style_key, style_label in styles.items():
            title = f"{v['name']} × {style_label}"
            checked = (style_key == "default")
            choices.append(
                questionary.Choice(
                    title=title,
                    value=(v["name"], style_key),
                    checked=checked,
                )
            )

    selected = questionary.checkbox(
        "勾选要试听的 (音色 × 风格) 组合（空格选/取消，回车确认）：",
        choices=choices,
    ).ask() or []

    if len(selected) > 20:
        ok = questionary.confirm(
            f"勾选 {len(selected)} 个组合，预计耗时 ~{len(selected) * 3} 秒，确认？",
            default=True,
        ).ask()
        if not ok:
            return []

    return list(selected)
```

- [ ] **Step 4: 运行测试**

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/pytest tests/test_voice_lab.py -v`
Expected: 11 passed

- [ ] **Step 5: Commit**

```bash
git -C /Users/hainingyu/Code/auto_audiobook add voice_lab.py tests/test_voice_lab.py
git -C /Users/hainingyu/Code/auto_audiobook commit -m "feat(voice_lab): combo selector with default selection and 20+ guard"
```

---

## Task 5: `_synthesize_previews` 并发合成

**Files:**
- Modify: `voice_lab.py`
- Test: `tests/test_voice_lab.py`

> 思路：每个组合临时改 `config.TTS_VOICES[language]` 与 `config.TTS_STYLES`，复用 `synthesize_chapters` 现有路径；为避免互相干扰，用 `asyncio.Lock` 串行调用 `synthesize_chapters`（每次调用内部仍并发，整体外部串行）。这是最小侵入做法，不改 synthesizer。

- [ ] **Step 1: 写失败测试 — 单个失败不影响其他**

追加到 `tests/test_voice_lab.py`：

```python
import asyncio

import pytest

from models import ChapterAudio, CleanedChapter
from voice_lab import PreviewItem, _synthesize_previews


@pytest.mark.asyncio
async def test_synthesize_previews_partial_failure(monkeypatch, tmp_path):
    """一个组合 raise，其他组合应正常返回。"""
    call_count = {"n": 0}

    async def fake_synthesize_chapters(chapters, language="zh"):
        call_count["n"] += 1
        # 第二次调用抛错
        if call_count["n"] == 2:
            raise RuntimeError("simulated TTS failure")
        # 否则返回一段假音频（最小 WAV 头 + 几字节 PCM）
        wav_bytes = _minimal_wav_bytes()
        return [ChapterAudio(title=chapters[0].title, track_num=1, audio_chunks=[wav_bytes])], None

    monkeypatch.setattr(vl_module, "synthesize_chapters", fake_synthesize_chapters)

    combos = [("茉莉", "default"), ("白桦", "default"), ("苏打", "default")]
    items = await _synthesize_previews("测试文本。", "zh", combos, tmp_path)

    assert len(items) == 3
    successes = [it for it in items if it.error is None]
    failures = [it for it in items if it.error is not None]
    assert len(successes) == 2
    assert len(failures) == 1
    assert failures[0].voice == "白桦"
    assert "simulated" in failures[0].error
    # 成功项要有可访问的 mp3 文件
    for it in successes:
        assert it.mp3_path and it.mp3_path.exists()


def _minimal_wav_bytes() -> bytes:
    """构造最小可解码的 WAV：44 字节标准头 + 几个采样点静音。"""
    import io
    import wave
    buf = io.BytesIO()
    with wave.open(buf, "wb") as w:
        w.setnchannels(1)
        w.setsampwidth(2)
        w.setframerate(22050)
        w.writeframes(b"\x00\x00" * 100)
    return buf.getvalue()
```

> 注：测试用 `pytest-asyncio`。先确认其已可用：

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/python -c "import pytest_asyncio; print(pytest_asyncio.__version__)"`

如果报 `ModuleNotFoundError`，则**先**执行下方步骤 1.5 安装；否则跳过：

- [ ] **Step 1.5: 如缺失，安装 pytest-asyncio**

```bash
/Users/hainingyu/Code/auto_audiobook/.venv/bin/pip install pytest-asyncio
```

并在 `tests/conftest.py` 末尾**追加**：

```python
import pytest

@pytest.fixture(scope="session")
def anyio_backend():
    return "asyncio"
```

并在仓库根 `pyproject.toml` 或新建 `pytest.ini` 中加入 asyncio 模式（避免每个测试都需 mark）。如已有 `pyproject.toml`，找到 `[tool.pytest.ini_options]` 段追加 `asyncio_mode = "auto"`；否则新建 `pytest.ini`：

```ini
[pytest]
asyncio_mode = auto
```

实际执行前先 `cat /Users/hainingyu/Code/auto_audiobook/pyproject.toml` 检查；按现状决定走 `pyproject.toml` 还是 `pytest.ini`。

- [ ] **Step 2: 运行测试,确认失败**

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/pytest tests/test_voice_lab.py::test_synthesize_previews_partial_failure -v`
Expected: ERROR `ImportError: cannot import name 'PreviewItem' from 'voice_lab'` 或 `_synthesize_previews`

- [ ] **Step 3: 实现 `PreviewItem` 与 `_synthesize_previews`**

在 `voice_lab.py` 顶部追加 imports：

```python
import asyncio
import logging
from dataclasses import dataclass
from pathlib import Path

from assembler import _concat_wav_chunks, export_to_mp3
from rule_cleaner import clean_chapters as rule_clean
from synthesizer import synthesize_chapters

logger = logging.getLogger(__name__)
```

在文件中合适位置（`_truncate_at_sentence` 之前）声明 dataclass：

```python
@dataclass
class PreviewItem:
    voice: str
    style: str
    style_label: str
    mp3_path: Path | None
    error: str | None
    duration_sec: float
```

在文件末尾追加：

```python
async def _synthesize_one(
    text: str,
    language: str,
    voice: str,
    style_key: str,
    style_label: str,
    out_path: Path,
    lock: asyncio.Lock,
) -> PreviewItem:
    """合成一个组合 → 临时 mp3。失败时返回带 error 的 PreviewItem。"""
    cleaned = rule_clean([("preview", [text])])
    # 临时切换全局 voice/style，再交给 synthesize_chapters
    # 用 lock 串行化以避免并发改写 config
    async with lock:
        original_voice = config.TTS_VOICES.get(language)
        original_styles = config.TTS_STYLES
        config.TTS_VOICES[language] = voice
        config.TTS_STYLES = config.TTS_STYLE_PRESETS[style_key]
        try:
            try:
                chapter_audios, _ = await synthesize_chapters(cleaned, language=language)
            except Exception as e:
                return PreviewItem(
                    voice=voice, style=style_key, style_label=style_label,
                    mp3_path=None, error=f"{type(e).__name__}: {e}", duration_sec=0.0,
                )
        finally:
            if original_voice is not None:
                config.TTS_VOICES[language] = original_voice
            config.TTS_STYLES = original_styles

    if not chapter_audios or not chapter_audios[0].audio_chunks:
        return PreviewItem(
            voice=voice, style=style_key, style_label=style_label,
            mp3_path=None, error="empty audio", duration_sec=0.0,
        )

    combined = _concat_wav_chunks(chapter_audios[0].audio_chunks)
    duration = len(combined) / 1000.0
    export_to_mp3(combined, out_path)
    return PreviewItem(
        voice=voice, style=style_key, style_label=style_label,
        mp3_path=out_path, error=None, duration_sec=duration,
    )


async def _synthesize_previews(
    text: str,
    language: str,
    combos: list[tuple[str, str]],
    tmp_dir: Path,
) -> list[PreviewItem]:
    """串行合成所有组合（每个 synthesize_chapters 内部仍并发）。"""
    labels = _style_labels(language)
    lock = asyncio.Lock()
    tasks = []
    for voice, style_key in combos:
        out_path = tmp_dir / f"preview_{voice}_{style_key}.mp3"
        tasks.append(
            _synthesize_one(
                text, language, voice, style_key,
                labels.get(style_key, style_key), out_path, lock,
            )
        )
    return await asyncio.gather(*tasks)
```

- [ ] **Step 4: 运行测试**

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/pytest tests/test_voice_lab.py -v`
Expected: 12 passed

- [ ] **Step 5: Commit**

```bash
git -C /Users/hainingyu/Code/auto_audiobook add voice_lab.py tests/test_voice_lab.py tests/conftest.py
git -C /Users/hainingyu/Code/auto_audiobook commit -m "feat(voice_lab): synthesize multiple combos to temp mp3 with per-combo error isolation"
```

如果 Step 1.5 修改了 `pyproject.toml` / `pytest.ini`，一并 add commit。

---

## Task 6: `_play_mp3` 跨平台播放（搬运 + 共享）

**Files:**
- Modify: `voice_lab.py`
- Modify: `main.py:466-481`

> 现有 `main.py:_play_mp3` 是私有函数，voice_lab 也需要播放。最佳实践：把它**搬到 voice_lab.py**，main.py 改为从 voice_lab 导入。voice_lab 是新的"音频试听"领域，承担该函数更合理。

- [ ] **Step 1: 在 voice_lab.py 末尾添加 `_play_mp3`**

```python
import platform
import subprocess


def _play_mp3(path: Path) -> None:
    """跨平台播放 MP3 文件。播放器缺失时打印警告并返回。"""
    system = platform.system()
    try:
        if system == "Darwin":
            subprocess.run(["afplay", str(path)], check=False)
        elif system == "Linux":
            subprocess.run(["mpg123", "-q", str(path)], check=False)
        elif system == "Windows":
            subprocess.run(["start", str(path)], shell=True, check=False)
        else:
            logger.warning(f"暂不支持在当前系统播放音频: {system}")
    except FileNotFoundError as e:
        logger.warning(f"播放器不可用 ({e}); 文件位于: {path}")
```

- [ ] **Step 2: 修改 `main.py` 改为从 voice_lab 导入**

在 `main.py:466-481` 删除整段 `_play_mp3` 函数定义。在文件顶部已有的 imports 区域附近**追加**：

```python
from voice_lab import _play_mp3 as _play_mp3  # noqa: F401  (provisional re-export until full migration in Task 7)
```

> 这个 re-export 是过渡：Task 7 会完全删除 `_preview_sample`（唯一旧调用方），届时 main.py 中这行也可以删除。但为了让本任务后 main.py 仍可独立运行（测试 isolation），先保留。

- [ ] **Step 3: 验证 main.py 仍可 import 不报错**

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/python -c "import main; print('ok')"`
Expected: `ok`

- [ ] **Step 4: 运行测试套件**

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/pytest tests/ -v`
Expected: 12 passed

- [ ] **Step 5: Commit**

```bash
git -C /Users/hainingyu/Code/auto_audiobook add voice_lab.py main.py
git -C /Users/hainingyu/Code/auto_audiobook commit -m "refactor(voice_lab): move _play_mp3 to voice_lab module"
```

---

## Task 7: `_interactive_compare` 菜单循环

**Files:**
- Modify: `voice_lab.py`

> 这是用户交互核心，不写自动化测试（菜单循环测试成本高、收益低）。手测验证。

- [ ] **Step 1: 实现 `_render_table` 辅助函数**

在 `voice_lab.py` 顶部追加 imports：

```python
from rich.console import Console
from rich.table import Table

console = Console()
```

文件末尾追加：

```python
def _render_table(items: list[PreviewItem], current: int | None) -> None:
    """打印对比室表格。current 为当前候选的索引（0-based），可为 None。"""
    table = Table(
        title="试听对比室",
        border_style="cyan",
        show_header=True,
        header_style="bold magenta",
    )
    table.add_column("#", style="dim", width=3)
    table.add_column("音色")
    table.add_column("风格")
    table.add_column("状态")
    table.add_column("时长")
    table.add_column("候选", justify="center")

    for idx, it in enumerate(items):
        if it.error:
            status = f"[red]✗ {it.error[:20]}[/red]"
            duration = "-"
        else:
            status = "[green]✓ 已生成[/green]"
            duration = f"{it.duration_sec:.1f}s"
        marker = "[yellow]★[/yellow]" if current == idx else ""
        table.add_row(
            str(idx + 1), it.voice, it.style_label, status, duration, marker,
        )

    console.print(table)
```

- [ ] **Step 2: 实现 `_interactive_compare`**

文件末尾追加：

```python
async def _interactive_compare(
    items: list[PreviewItem], language: str,
) -> tuple[str, str]:
    """菜单循环：选听 / 设候选 / 确认。返回 (voice, style)。

    退出（Ctrl+C / 取消）会抛 KeyboardInterrupt，由调用方决定回退。
    """
    # 初始候选 = 第一个成功项
    current = next(
        (i for i, it in enumerate(items) if it.error is None),
        None,
    )
    if current is None:
        # 全部失败，回退到默认
        console.print("[yellow]全部试听合成失败，使用默认配置继续[/yellow]")
        return (
            config.TTS_VOICES.get(language, "茉莉"),
            "default",
        )

    while True:
        _render_table(items, current)
        cur_item = items[current]
        console.print(
            f"[cyan]当前候选：#{current + 1} {cur_item.voice} × {cur_item.style_label}[/cyan]"
        )

        action = await questionary.select(
            "下一步操作：",
            choices=[
                questionary.Choice(title="▶ 试听某个编号", value="play"),
                questionary.Choice(title="★ 设某个编号为候选", value="pick"),
                questionary.Choice(title="✓ 确认当前候选并继续", value="confirm"),
                questionary.Choice(title="✗ 取消（用默认音色/风格）", value="cancel"),
            ],
        ).unsafe_ask_async()

        if action == "confirm":
            return (cur_item.voice, cur_item.style)

        if action == "cancel":
            return (
                config.TTS_VOICES.get(language, "茉莉"),
                "default",
            )

        if action in ("play", "pick"):
            playable_indices = [
                str(i + 1) for i, it in enumerate(items) if it.error is None
            ]
            num_str = await questionary.select(
                "选择编号:",
                choices=playable_indices,
            ).unsafe_ask_async()
            n = int(num_str) - 1
            if action == "play":
                console.print(
                    f"[cyan]播放 #{n + 1} ({items[n].duration_sec:.1f}s)...[/cyan]"
                )
                _play_mp3(items[n].mp3_path)
            else:  # pick
                current = n
                console.print(f"[green]候选已切换到 #{n + 1}[/green]")
```

- [ ] **Step 3: 实现入口 `run_voice_lab`**

文件末尾追加：

```python
import shutil
import tempfile

from parser import parse_file


async def run_voice_lab(file_path: Path, language: str) -> tuple[str, str]:
    """试听对比室入口。返回最终选定的 (voice, style)。

    任意失败均回退到 (默认音色, 'default')，不阻塞主流程。
    """
    default_voice = config.TTS_VOICES.get(language, "茉莉")
    default_fallback = (default_voice, "default")

    # 解析书籍
    try:
        book = parse_file(file_path)
    except Exception as e:
        console.print(f"[yellow]试听准备失败: {e}，使用默认配置继续[/yellow]")
        return default_fallback

    # 抽取试听文本
    text = _sample_preview_text(book, target_chars=200)
    if not text:
        console.print("[yellow]无法抽取试听文本，使用默认配置继续[/yellow]")
        return default_fallback

    preview_clip = text[:80] + "…" if len(text) > 80 else text
    console.print(f"[dim]试听文本: {preview_clip}[/dim]")

    # 用户勾选组合
    combos = _select_combos(language)
    if not combos:
        console.print("[yellow]未勾选任何组合，使用默认配置继续[/yellow]")
        return default_fallback

    # 在临时目录合成
    tmp_dir = Path(tempfile.mkdtemp(prefix="voice_lab_"))
    try:
        console.print(f"[cyan]开始合成 {len(combos)} 个试听片段…[/cyan]")
        items = await _synthesize_previews(text, language, combos, tmp_dir)
        try:
            return await _interactive_compare(items, language)
        except KeyboardInterrupt:
            console.print("[yellow]已取消试听，使用默认配置继续[/yellow]")
            return default_fallback
    finally:
        shutil.rmtree(tmp_dir, ignore_errors=True)
```

- [ ] **Step 4: 验证 voice_lab 整模块可 import**

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/python -c "from voice_lab import run_voice_lab; print('ok')"`
Expected: `ok`

- [ ] **Step 5: 运行所有现有测试，确认无回归**

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/pytest tests/ -v`
Expected: 12 passed

- [ ] **Step 6: Commit**

```bash
git -C /Users/hainingyu/Code/auto_audiobook add voice_lab.py
git -C /Users/hainingyu/Code/auto_audiobook commit -m "feat(voice_lab): interactive comparison loop and run_voice_lab entry point"
```

---

## Task 8: 整合到 `main.py` 替换旧试听流程

**Files:**
- Modify: `main.py:439-455`（删除 `_select_voice`）
- Modify: `main.py:364-371`（删除 `_select_style`）
- Modify: `main.py:484-537`（删除 `_preview_sample`）
- Modify: `main.py:466-481` 已在 Task 6 移除/改为 import；最终在此清理过渡 import
- Modify: `main.py:579-606`（`interactive_mode` 内的"选音色 → 选风格 → 试听"段）

- [ ] **Step 1: 删除 `_select_voice`**

打开 `main.py`，删除整个函数 `def _select_voice(...)`（439-455 行附近）。

- [ ] **Step 2: 删除 `_select_style`**

删除整个函数 `def _select_style(...)`（364-371 行附近）。

- [ ] **Step 3: 删除 `_preview_sample`**

删除整个 `async def _preview_sample(...)`（484-537 行附近）及其相关 imports（如顶部 `import tempfile` 若不再有其他用途也保留无害；不主动删）。

- [ ] **Step 4: 替换 `interactive_mode` 中的对应段**

定位到（约 590-606 行）这一段：

```python
console.print(f"\n[bold]{t('confirm_language')}[/bold]")
language = _confirm_language(selected, style)

console.print(f"\n[bold]选择朗读者音色[/bold]")
voice = _select_voice(style, language)

console.print(f"\n[bold]{t('select_style')}[/bold]")
style_choice = _select_style(style)
_apply_processing_config(language, style_choice, voice)

_show_pending_summary(selected, language, style_choice, voice)

# 试听预览
console.print("\n[bold]试听预览...[/bold]")
preview_ok = asyncio.run(_preview_sample(selected[0], language, style_choice))
if not preview_ok:
    console.print("[yellow]试听未通过，请重新选择风格或音色后重试[/yellow]")
    continue
```

替换为：

```python
console.print(f"\n[bold]{t('confirm_language')}[/bold]")
language = _confirm_language(selected, style)

import voice_lab
console.print("\n[bold]进入试听对比室…[/bold]")
voice, style_choice = asyncio.run(voice_lab.run_voice_lab(selected[0], language))
_apply_processing_config(language, style_choice, voice)

_show_pending_summary(selected, language, style_choice, voice)
```

- [ ] **Step 5: 清理 Task 6 留下的过渡 import**

如果 main.py 中仍有 `from voice_lab import _play_mp3 as _play_mp3` 这行（Task 6 添加的），现在 main.py 已经不再调用 `_play_mp3`，可以删掉。

- [ ] **Step 6: 验证 main.py 可 import 且 CLI 不爆**

```bash
cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/python -c "import main; print('ok')"
cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/python main.py --help
```
Expected: `ok`；`--help` 输出 argparse 标准 usage，无 traceback。

- [ ] **Step 7: 运行测试套件**

Run: `cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/pytest tests/ -v`
Expected: 12 passed

- [ ] **Step 8: Commit**

```bash
git -C /Users/hainingyu/Code/auto_audiobook add main.py
git -C /Users/hainingyu/Code/auto_audiobook commit -m "refactor(main): replace inline preview steps with voice_lab.run_voice_lab"
```

---

## Task 9: 端到端手测

**Files:** N/A（人工验收，不改代码）

- [ ] **Step 1: 准备一本 EPUB 放到 `input/`**

从 `input/` 已有书籍中任选一本（用户机器上应有），或临时放一本测试。

- [ ] **Step 2: 启动交互式模式**

```bash
cd /Users/hainingyu/Code/auto_audiobook && .venv/bin/python main.py
```

- [ ] **Step 3: 验收清单**

依次执行流程，对照 spec 验收标准：

- [ ] 试听文本提示行**不是**版权页/目录文字（应为正文片段）
- [ ] 勾选阶段默认勾选"所有音色 × 默认风格"
- [ ] 合成完毕后表格能显示所有勾选项的状态、时长
- [ ] 选"试听某个编号"播放对应片段，播放完返回菜单
- [ ] "设某个编号为候选"会移动 ★ 标记
- [ ] "确认当前候选"后流程继续到 batch 处理（中断 batch 不影响验收）
- [ ] 在试听阶段按 Ctrl+C 不崩溃，能回到主菜单或继续走默认配置

- [ ] **Step 4: 如有问题回报**

将异常步骤记录到 `progress.txt`（与 plan 同目录），方便后续修订。无问题则跳到 Step 5。

- [ ] **Step 5: 关闭里程碑**

```bash
git -C /Users/hainingyu/Code/auto_audiobook tag -a voice-lab-v1 -m "Voice Lab feature complete"
```

（如果你不打 tag 也无所谓，跳过即可。）

---

## 验收 / 完成条件

| spec 验收标准 | 对应任务 |
|---|---|
| 试听文本不来自版权页/目录 | Task 3 + Task 9 |
| 一次会话内可生成 ≥ 2 组合并自由切换 | Task 5 + Task 7 + Task 9 |
| 单组合失败不阻塞其他 / 主流程 | Task 5（自动化测试覆盖）+ Task 7（fallback） |
| 退出试听后能进入正常 batch_process | Task 8 + Task 9 |
| `tests/test_voice_lab.py` 全部通过 | Task 2-5（每任务结尾 pytest） |

---

## Self-Review 备注

- **类型一致性**：全程使用 `BookData`（不是 `Book`）；`_synthesize_previews` 签名 `(text, language, combos, tmp_dir)` 在 Task 5 定义、Task 7 调用一致；`PreviewItem` 字段 `mp3_path/error/duration_sec/style_label` 在 Task 5 定义、Task 7 渲染时一致。
- **占位符检查**：每个代码步骤含完整代码；测试代码含具体断言；命令含期望输出片段。无 "TBD/TODO/Add appropriate"。
- **spec 覆盖**：架构表 / 数据流 / 试听文本采样 / 对比室交互 / 错误处理 / 测试 / 验收标准 — 均有对应任务。
- **顺序合理性**：每任务都以 commit 收尾；任务 6（搬运 _play_mp3）刻意安排在 Task 7（用到 _play_mp3）之前；Task 8 整合放在最后避免中途破坏 main.py。
